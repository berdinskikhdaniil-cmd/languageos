/**
 * Until goals are user-configurable, the weekly chart draws this daily target.
 * It is a display default, not a stored preference and not user data.
 */
export const DEFAULT_DAILY_GOAL_MINUTES = 45;
